<!DOCTYPE html>
<html>
<head>
  <title>Marketplace Home</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-4">
  <h2> Recent Jobs</h2>
  <ul class="list-group">
    {% for job in recent_jobs %}
      <li class="list-group-item">
        <a href="/job/{{ job[0] }}">{{ job[1][:50] }}...</a>
      </li>
    {% endfor %}
  </ul>

  <h2 class="mt-4">Top Active Jobs</h2>
  <ul class="list-group">
    {% for job in top_active_jobs %}
      <li class="list-group-item">
        <a href="/job/{{ job[0] }}">{{ job[1][:50] }}... ({{ job[2] }} bids)</a>
      </li>
    {% endfor %}
  </ul>

  <a href="/post-job" class="btn btn-primary mt-4">Post a New Job</a>
</body>
</html>